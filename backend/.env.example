# Banco de dados
DATABASE_URL=sqlite+aiosqlite:///./flowday.db

# Ambiente
APP_ENV=production

# CORS — origens permitidas (formato: array JSON)
ALLOWED_ORIGINS=["https://nicoryy.github.io","http://localhost:5173"]
